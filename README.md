# AD-AutoUnlock-Tool
This tool uses PowerShell to go into active directory and mass unlock accounts (every 5 minutes by default.) It also keeps a log of the accounts unlocked, time stamp when unlocked and when they where locked.
